# This Module Takes The input from the Main File(Model.py) and then extract the features From the text and return them to the main file(Model) 
from transformers import AutoTokenizer,AutoModelForCausalLM
from transformers import pipeline

def FindFeatures(Query) -> str:
    return
    